spring:
  application:
    name: loan-proposal-query
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  mongodb:
    uri: mongodb://localhost:27017/loan_proposal_query

server:
  port: 8082
